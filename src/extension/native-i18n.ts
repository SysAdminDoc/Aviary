/**
 * Browser-owned extension copy. The English values are also catalog keys, so the build can
 * resolve every locale from the same translation table used by the Control Center.
 */
export const NATIVE_I18N_COPY = {
  extensionName: "Aviary for X",
  extensionDescription:
    "Local-first X/Twitter enhancer with reversible controls and privacy-first defaults.",
  actionTitle: "Aviary",
  contextDownloadMedia: "Download media with Aviary"
} as const;
